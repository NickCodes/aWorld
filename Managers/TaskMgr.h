class TaskManager
{
	
public:
	TaskManager(void);
    ~TaskManager(void);
	
	void subscribe(void);			// Function to add what needs updating per frame to a list of some sort
	
private:
			
protected:

};
